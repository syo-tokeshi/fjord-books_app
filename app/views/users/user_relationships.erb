<h3><%= @title %></h3>
<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>
<% end %>
<%= link_to t('views.common.back'), user_path(@user) %>
